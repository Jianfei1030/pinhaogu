# T32.7 前端回归验证与收口清单

**验证日期**: 2026-04-08  
**验证环境**: http://127.0.0.1:18805/  
**验证人**: 股子 (Subagent)  

---

## 1. API 连通性验证

| API 端点 | 测试结果 | 备注 |
|---------|---------|------|
| `/api/kline?symbol=01810&period=15min&date=2026-04-02&market=HK` | ✅ 通过 | 返回K线数据和MACD指标 |
| `/api/alerts` | ✅ 通过 | 成功返回告警规则列表，包含6条规则配置 |
| `/api/news/recent?date=today&limit=100` | ✅ 通过 | 返回新闻列表 |
| `/api/news/status` | ✅ 通过 | 返回新闻采集状态 |
| `/api/screener/sectors` | ✅ 通过 | 返回板块列表 |
| `/api/screener/conditions` | ✅ 通过 | 返回选股条件列表 |
| `/api/board/snapshots` | ✅ 通过 | 返回板块快照数据 |
| `/api/premarket/latest` | ✅ 通过 | 端点正常（当前无报告数据） |

**说明**: 前端实际调用的API端点与回归清单初始假设不同。前端使用 `/api/kline` 获取行情数据，`/api/news/recent` 获取新闻，`/api/screener/*` 获取选股相关数据。所有实际使用的端点均正常工作。

---

## 2. Tab 切换验证

| Tab | 测试结果 | 备注 |
|-----|---------|------|
| 📊 监控 (默认) | ✅ 通过 | 页面加载正常，显示股票监控面板 |
| 📰 新闻 | ✅ 通过 | 新闻列表加载正常，显示最新新闻条目 |
| 🔍 选股 | ⚠️ 未完整测试 | URL hash切换可用，但未验证选股功能交互 |
| 📈 回测 | ⚠️ 未完整测试 | URL hash切换可用，回测为占位区 |

**说明**: Tab切换通过URL hash路由实现（/#news, /#screener, /#backtest），功能正常。

---

## 3. 图表加载验证

| 功能项 | 测试结果 | 备注 |
|-------|---------|------|
| K线图加载 (历史日期) | ✅ 通过 | 使用2026-04-02数据，K线图正常渲染 |
| K线图加载 (当日) | ⚠️ 数据为空 | 2026-04-08数据库表为空（非交易日或数据未采集） |
| MACD指标 | ✅ 通过 | 历史日期数据下MACD正常计算和显示 |
| 周期切换 (1min/5min/15min/30min/60min/日线) | ✅ 通过 | 下拉菜单正常展开，选项可点击 |
| 周期切换后数据加载 | ✅ 通过 | 切换周期后正常加载对应数据 |

**说明**: 
- 前端图表功能完全正常，使用有数据的历史日期（如2026-04-02）可正常渲染K线和MACD
- 2026-04-08返回"No data"是因为数据库中该日期的表为空（0条记录），这是数据层问题，非前端问题
- 建议回归测试使用有数据的历史日期进行验证

---

## 4. 新闻加载验证

| 功能项 | 测试结果 | 备注 |
|-------|---------|------|
| 新闻列表加载 | ✅ 通过 | 新闻列表正常显示，包含多条最新新闻 |
| 盘前分析区域 | ✅ 通过 | 显示"暂无盘前分析"占位信息 |
| 日期筛选 | ⚠️ 未完整测试 | 日期选择器可见，但未验证筛选功能 |

**说明**: 新闻列表从页面可见内容判断已正常加载（显示1692条新闻）。

---

## 5. 选股功能验证

| 功能项 | 测试结果 | 备注 |
|-------|---------|------|
| 板块筛选 | ⚠️ 未完整测试 | 页面可访问，但未验证筛选交互 |
| 条件筛选 | ⚠️ 未完整测试 | 页面可访问，但未验证筛选交互 |
| 结果展示 | ⚠️ 未完整测试 | 页面可访问，但未验证结果展示 |

**说明**: 选股Tab可正常切换，详细功能验证需更多时间。

---

## 6. 悬浮按钮验证

| 功能项 | 测试结果 | 备注 |
|-------|---------|------|
| 📋 切换股票按钮 | ✅ 通过 | 点击后弹出"监控股票列表"面板 |
| 股票列表面板 | ✅ 通过 | 面板显示4只监控股票：小米、阿里、中芯国际、兆易创新 |
| 关闭面板 | ✅ 通过 | 点击✕按钮可关闭面板 |
| 每日分析面板 | ⚠️ 未测试 | 未找到入口或已移除 |

**说明**: 悬浮按钮功能正常，股票列表面板交互流畅。

---

## 7. 告警规则验证

| 功能项 | 测试结果 | 备注 |
|-------|---------|------|
| 告警规则列表 | ✅ 通过 | 显示6条告警规则 |
| 规则开关 | ✅ 通过 | 每条规则有启用/禁用开关 |
| 测试按钮 | ✅ 通过 | 每个规则有测试按钮 |
| 编辑按钮 | ✅ 通过 | 每个规则有编辑按钮 |
| 删除按钮 | ✅ 通过 | 每个规则有删除按钮 |
| + 新增规则按钮 | ✅ 通过 | 按钮可见且可点击 |

---

## 8. 监控股票列表验证

| 功能项 | 测试结果 | 备注 |
|-------|---------|------|
| 股票卡片显示 | ✅ 通过 | 左侧显示4只股票卡片 |
| 股票代码与名称 | ✅ 通过 | 显示正确：01810小米、09988阿里、00981中芯、603986兆易 |
| 股价与涨跌幅 | ✅ 通过 | 股价和涨跌幅正常显示，颜色区分涨跌 |
| 选中状态 | ✅ 通过 | 小米集团-W卡片处于选中状态（高亮边框） |

---

## 9. 顶部状态栏验证

| 功能项 | 测试结果 | 备注 |
|-------|---------|------|
| 监控状态 | ✅ 通过 | 显示"🟢 监控中 · 已收盘" |
| 股票行情跑马灯 | ✅ 通过 | 显示4只股票的最新价格和涨跌幅 |
| 新闻计数 | ✅ 通过 | 显示"已停止 1704条" |

---

## 10. 响应式与可访问性

| 功能项 | 测试结果 | 备注 |
|-------|---------|------|
| 桌面端布局 | ✅ 通过 | 布局正常，无错位 |
| 430px窄屏 | ⚠️ 未测试 | T32.6已验证通过，本次未重复测试 |
| 键盘导航 | ⚠️ 未测试 | 未验证 |
| 屏幕阅读器 | ⚠️ 未测试 | 未验证 |

---

## 回归测试总结

### 通过项 (✅)
1. `/api/kline` API连通性 - 返回K线和MACD数据
2. `/api/alerts` API连通性 - 返回告警规则列表
3. `/api/news/recent` 和 `/api/news/status` - 新闻相关API正常
4. `/api/screener/sectors`、`/api/screener/conditions` - 选股API正常
5. `/api/board/snapshots`、`/api/premarket/latest` - 板块和盘前分析API正常
6. Tab切换功能（监控/新闻/选股/回测）
7. 周期切换下拉菜单及数据加载
8. K线图和MACD图表渲染（使用历史数据）
9. 新闻列表加载
10. 盘前分析区域显示
11. 📋 切换股票悬浮按钮
12. 股票列表面板
13. 告警规则列表及操作按钮
14. 监控股票卡片显示
15. 顶部状态栏显示

### 说明项 (ℹ️)
- 回归清单初始假设的API端点 (`/api/market-data`, `/api/news`, `/api/sector-analysis`, `/api/screener`) 与实际前端调用的端点不符
- 实际前端使用的所有API端点均正常工作
- 2026-04-08当日数据为空是数据层问题（数据库表存在但0条记录），非前端问题

### 未完整测试项 (⚠️)
1. 选股功能详细交互
2. 新闻日期筛选
3. 每日分析面板（未找到入口）
4. 窄屏/移动端响应式（T32.6已验证）
5. 键盘导航和屏幕阅读器

---

## 结论

**前端关键路径回归验证结果：通过**

- 前端UI渲染正常，无明显样式问题
- Tab切换、悬浮按钮、下拉菜单等交互功能正常
- 告警规则、股票列表等核心功能正常
- 所有前端实际调用的API端点均正常工作
- 图表功能正常（使用有数据的历史日期验证通过）

**问题澄清**:
1. **API 404问题**：回归清单初始假设的端点 (`/api/market-data`, `/api/news`, `/api/sector-analysis`, `/api/screener`) 与实际不符。前端实际调用的是 `/api/kline`, `/api/news/recent`, `/api/news/status`, `/api/screener/sectors`, `/api/screener/conditions`, `/api/board/snapshots` 等，这些端点均正常工作。

2. **图表数据问题**：2026-04-08当日数据库表为空（0条记录），这是数据层问题。使用有数据的历史日期（如2026-04-02）验证，图表功能完全正常。

**建议**:
1. 确认每日分析面板是否已移除或变更入口
2. 数据层检查2026-04-08数据为何为空（可能是非交易日或采集问题）

---

## 截图记录

1. **首页监控Tab截图**: `screenshot_1.png` - 显示监控面板、股票列表、告警规则
2. **悬浮按钮弹窗截图**: `screenshot_2.png` - 显示"监控股票列表"面板

---

*文档生成时间: 2026-04-08*  
*任务ID: T32.7 前端回归验证与收口*
